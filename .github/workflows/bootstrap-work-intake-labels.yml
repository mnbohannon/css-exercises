name: Bootstrap work intake labels

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/bootstrap-work-intake-labels.yml"

permissions:
  contents: read
  issues: write

jobs:
  labels:
    uses: mnbohannon/.github/.github/workflows/bootstrap-work-intake-labels.yml@main
